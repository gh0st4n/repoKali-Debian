# repoKali-Debian
setup kali linux repo on debian

# Requirement
- Debian Based

# Setup
```bash
chmod +x setup-kali-repo.sh
sudo ./setup-kali-repo.sh
```

# How To Use
```bash
sudo apt install -t kali-rolling [Package]
```
